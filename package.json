{
  "name": "resume-site",
  "version": "1.0.0",
  "description": "Bryan Tan's Resume Website",
  "main": "server.js",
  "scripts": {
    "build": "mkdir -p build && ejs views/index.ejs -f data.json -o build/index.html && ejs views/about.ejs -f data.json -o build/about.html && ejs views/resume.ejs -f data.json -o build/resume.html && cp -r public/* build/",
    "deploy": "npm run build && npx gh-pages -d build"
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-ejs-layouts": "^2.5.0",
    "gh-pages": "^3.2.3",
    "ejs-cli": "^0.3.3"
  },
  "devDependencies": {},
  "engines": {
    "node": "14.x"
  },
  "author": "",
  "license": "ISC"
}